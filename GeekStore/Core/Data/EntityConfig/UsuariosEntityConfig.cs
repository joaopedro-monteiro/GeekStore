using Microsoft.EntityFrameworkCore;
using Microsoft.EntityFrameworkCore.Metadata.Builders;

namespace GeekStore.Core.Data.EntityConfig
{
    public class UsuariosEntityConfig /*: /*IEntityTypeConfiguration<NovaConta>*/
    {
        //public void Configure(EntityTypeBuilder<NovaConta> builder)
        //{
        //    builder.ToTable("Usuarios");

        //    builder.HasKey(x => x.Id);
        //    builder.Property(x => x.Nome).HasMaxLength(50).IsRequired();
        //    builder.Property(x => x.Sobrenome).HasMaxLength(50).IsRequired();
        //    builder.Property(x => x.DataDeNascimento).IsRequired();
        //    builder.Property(x => x.Celular).IsRequired();
        //    builder.Property(x => x.Email).IsRequired();
        //    builder.Property(x => x.Senha).IsRequired();
        //}
    }
}
